/*
Cho hai số nguyên (có thể âm) có giá trị tuyệt đối không quá 106.
Viết chương trình tính tổng hai số 
*/

#include <stdio.h>

int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    printf("%d", a + b);
    return 0;
}